WITH MEMBER [Measures].[AVG_occupancy] AS [Measures].[Sum Occupancy]/[Measures].[Route Occupancy Rate Count] 
SELECT 
	NON EMPTY { [Measures].[AVG_occupancy] } ON COLUMNS, 
	NON EMPTY Order({ ([Routes].[Departure Hub End Name].[Departure Hub End Name].ALLMEMBERS ) }, [Measures].[AVG_occupancy], DESC)ON ROWS 
FROM [Railway Station DW]
